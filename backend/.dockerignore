node_modules
dist
npm-debug.log
*.log
.git
.gitignore
README.md
Dockerfile
.dockerignore
# Note: .env files are intentionally NOT ignored so they can be copied into the Docker image
# The Dockerfile will copy .env.production and other .env files as needed
